/**
 * 주어진 시간표와 제약 조건을 바탕으로 알바 가능한 시간대를 찾는 함수
 *
 * @param {Array} schedule - 강의 시간표 배열
 * @param {Object} constraints - 제약 조건 객체
 * @returns {Array} - 알바 가능한 시간대 배열
 */
export function findWorkableSlots(schedule, constraints) {
  const slots = [];
  return slots;
}
